# Cloudflare Worker API (required on Vercel — baked in at build time).
# Local dev defaults to http://127.0.0.1:8787 when unset.
PUBLIC_API_URL=https://anvesha-merch-api.tech4anvesha.workers.dev

# Public site origin, used for links handed to someone else (the distribution
# counter link). Change this when the domain does.
PUBLIC_SITE_URL=https://anvesha-26.vercel.app
